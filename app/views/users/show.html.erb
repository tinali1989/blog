<h1>User Information</h1>
<hr>

<%= error_messages_for :user %>

<% form_for(@user) do |f| %>
  <p>
    <b>Username:</b>
    <%=h @user.username %>
  </p>

  <p>
    <b>Password:</b>
    <%=h @user.password %>
  </p>

  <p>
    <b>Role：</b>
     <%=h @user.role == '1' ? 'Admin':'Customer' %>
  </p>
<% end %>

<%= link_to 'Back', users_path %>
